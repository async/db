/* eslint-disable */
// This file is generated by db. Do not edit by hand.

/** Advanced settings atlas loaded from ../docs/advanced/*.md. */
export type Advanced = {
  /** Stable page id from the markdown filename. */
  id: string;
  /** Raw markdown body. Rendering is site-owned. */
  body: string;
  /** Page title derived from the first ATX heading. */
  title?: string;
  /** URL slug, equal to the page id. */
  slug?: string;
  /** Navigation tier from the schema registry. */
  tier?: string;
  /** Sort order inside the navigation tier. */
  navOrder?: number;
  /** Short page summary from the schema registry. */
  description?: string;
  /** One-minute minimum reading-time estimate from the raw body. */
  readingTimeMinutes?: number;
  /** Built HTML path relative to the site root. */
  htmlPath?: string;
};

/** Documentation pages loaded from the curated ../docs markdown allowlist. */
export type Page = {
  /** Stable page id from the markdown filename. */
  id: string;
  /** Raw markdown body. Rendering is site-owned. */
  body: string;
  /** Page title derived from the first ATX heading. */
  title?: string;
  /** URL slug, equal to the page id. */
  slug?: string;
  /** Navigation tier from the schema registry. */
  tier?: string;
  /** Sort order inside the navigation tier. */
  navOrder?: number;
  /** Short page summary from the schema registry. */
  description?: string;
  /** One-minute minimum reading-time estimate from the raw body. */
  readingTimeMinutes?: number;
  /** Built HTML path relative to the site root. */
  htmlPath?: string;
};

export type DbCollections = {
  advanced: Advanced;
  pages: Page;
};

export type DbDocuments = {
};

export type DbTypes = {
  collections: DbCollections;
  documents: DbDocuments;
};

export type DbCollectionName = keyof DbCollections;
export type DbDocumentName = keyof DbDocuments;
